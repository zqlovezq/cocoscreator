import { Color, Graphics, Layers, Node, Rect, Vec2, Vec3, sp, sys } from "cc";
import { tab } from "../Table/table_gen";

/** 模块相关 */
export class ModuleUtil {

    static checkIsOpen(module: tab.Module) {
        return true
    }
}

