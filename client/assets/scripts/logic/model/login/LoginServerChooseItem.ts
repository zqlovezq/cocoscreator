import { _decorator, Component, Label, log, Node } from 'cc';
import { ServerChooseItem } from '../common/ServerChooseItem';
const { ccclass, property } = _decorator;

@ccclass('LoginServerChooseItem')
export class LoginServerChooseItem extends ServerChooseItem {

    onTcouhItem(): void {
        
    }
}